// Placeholder route shell. The generation agent overwrites this with the
// real HashRouter + navigation derived from ProductSpec.views.
export default function App() {
  return (
    <main className="min-h-screen flex items-center justify-center text-foreground">
      <p className="text-neutral">尚未生成内容。</p>
    </main>
  );
}
